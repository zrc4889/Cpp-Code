#include <bits/stdc++.h>
using namespace std;

int main() {
	freopen("squ.in", "r", stdin);
	freopen("squ.out", "w", stdout);
    int a, b, c;
    cin >> a >> b >> c;
    cout << (a / c) * (b / c);
    return 0;
}