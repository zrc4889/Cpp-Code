#include <bits/stdc++.h>
using namespace std;
int toOct(int n)
{
    int base = 1;
    while (n)
    {
        t = t * 10 + n % 
        base *= 8;
    }
}
int main()
{
#ifdef LOCAL
    LOCALfo
#endif
        ;
    int n;
    cin >> n;
    toOct(n);
    return 0;
}