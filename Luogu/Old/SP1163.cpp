#include <bits/stdc++.h>
using namespace std;

bool checkL(char a)
{
    return 'a' <= a && a <= 'z';
}

bool checkU(char a)
{
    return 'A' <= a && a <= 'Z';
}

void solve(string str)
{
    int n = str.length() - 1;
    for (int i = 0; i < n - 1; i++)
    {
        
    }
}

int main()
{
#ifdef LOCAL
    LOCALfo
#endif
        ;
    string s;
    while (cin >> s)
    {
        solve(s);
        cout << endl;
    }
    return 0;
}