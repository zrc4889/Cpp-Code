#include <bits/stdc++.h>
using namespace std;
const int _ = 1e6 + 10;
int a[_], b[_];

int main()
{
#ifdef LOCAL
    LOCALfo
#endif
        ;
    int n;
    cin >> n;

    for (int i = 1; i <= n; i++)
        cin >> a[i] >> b[i];
    
    

    return 0;
}